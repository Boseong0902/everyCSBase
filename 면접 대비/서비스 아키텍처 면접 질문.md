# 서비스 아키텍처 면접 질문

## 모놀리식 vs MSA

관련 개념: [[모놀리식 vs MSA]], [[서비스 아키텍처]]

- 모놀리식 아키텍처란 무엇인가?
- MSA란 무엇인가?
- 모놀리식과 MSA는 서비스를 나누는 기준에서 어떻게 다른가?
- 기능별 독립 서비스가 서로 통신하는 구조의 장점과 단점은 무엇인가?
- 서비스를 나누면 배포와 장애 격리 측면에서 어떤 이점이 생기는가?
- 서비스를 나누면 데이터 일관성 측면에서 어떤 비용이 생기는가?
- MSA에서 서비스마다 DB를 나누면 어떤 문제가 새로 생기는가?
- 처음부터 MSA로 시작하지 않는 것이 나을 수 있는 이유는 무엇인가?

## 분산 트랜잭션

관련 개념: [[분산 트랜잭션]], [[이벤트 리스너와 트랜잭션]], [[모놀리식 vs MSA]]

- 분산 트랜잭션은 어떤 문제에서 출발하는가?
- 여러 서비스의 작업을 하나의 트랜잭션으로 묶지 못하는 이유는 무엇인가?
- 외부 API 호출에는 왜 롤백이라는 개념이 성립하지 않는가?
- 2PC는 어떻게 동작하며 MSA에서 잘 쓰이지 않는 이유는 무엇인가?
- 보상 트랜잭션(Saga)이란 무엇인가?
- Saga의 보상 트랜잭션 자체가 실패하면 어떻게 하는가?
- Outbox 패턴은 어떤 유실 문제를 막는가?
- Outbox 패턴에서 이벤트 기록이 원자적으로 묶이는 이유는 무엇인가?
- 메시지 큐로 후속 작업을 처리할 때 멱등성이 왜 필요한가?
- 최종적 일관성을 받아들인다는 말은 설계 관점에서 무슨 뜻인가?
- `@TransactionalEventListener(AFTER_COMMIT)`만으로 정합성이 해결되지 않는 이유는 무엇인가?

## 스프링 프로파일

관련 개념: [[개발 환경과 운영 환경 분리하는 방법 - 스프링 프로파일]]

- 개발 환경과 운영 환경을 분리해야 하는 이유는 무엇인가?
- Spring Boot에서 프로파일별 설정 파일은 어떻게 구성하는가?
- `application.yml`, `application-dev.yml`, `application-prod.yml`의 역할은 무엇인가?
- `SPRING_PROFILES_ACTIVE`는 무엇을 제어하는가?
- `@Profile`을 코드 레벨에서 사용하면 어떤 구현을 분리할 수 있는가?
- 프로파일별 설정은 어떤 우선순위로 병합되는가?
- 운영 DB 비밀번호를 설정 파일에 평문으로 두면 어떤 문제가 있고, 대안은 무엇인가?

## 페일오버

관련 개념: [[페일오버]], [[MySQL 페일오버 설계 방식]], [[PostgreSQL 페일오버 설계 방식]]

- 페일오버란 무엇인가?
- 액티브-스탠바이 페일오버란 무엇인가?
- 액티브-액티브 페일오버란 무엇인가?
- 페일오버와 무중단 배포는 어떤 점에서 비슷하고 어떤 점에서 다른가?
- 기존 트래픽을 다른 서버가 그대로 이어받기 어려운 이유는 무엇인가?
- 액티브-스탠바이와 액티브-액티브 중 무엇을 선택할지는 어떤 기준으로 판단하는가?
- 장애를 감지하는 하트비트와 헬스체크는 어떻게 동작하는가?
- 스플릿 브레인이란 무엇이고 왜 위험한가?

## VIP 페일오버

관련 개념: [[페일오버]]

- VIP 페일오버란 무엇인가?
- IP 주소와 MAC 주소의 차이는 무엇인가?
- ARP 테이블은 어떤 역할을 하는가?
- GARP는 페일오버 상황에서 어떤 역할을 하는가?
- VIP 페일오버가 동일 L2 네트워크 안에서 동작해야 하는 이유는 무엇인가?
- VIP 페일오버에서 클라이언트가 DB 장애를 인지조차 못 하는 이유는 무엇인가?
- 대부분의 클라우드가 VIP 대신 DNS 페일오버를 제공하는 이유는 무엇인가?

## DNS 페일오버

관련 개념: [[페일오버]]

- DNS 페일오버란 무엇인가?
- DNS 페일오버에서 TTL은 어떤 문제를 만들 수 있는가?
- 클라이언트 로컬 캐시에 도메인-IP 매핑이 남아 있으면 어떤 일이 생기는가?

## MySQL 페일오버

관련 개념: [[MySQL 페일오버 설계 방식]]

- Primary와 Replica를 동일 VPC의 다른 AZ에 배치하는 이유는 무엇인가?
- MySQL에서 GTID는 페일오버에 어떤 도움을 주는가?
- 반동기 복제란 무엇인가?
- 반동기 복제를 쓰면 데이터 유실이 완전히 사라지는가?
- InnoDB Cluster는 Primary 장애를 어떻게 감지하는가?
- InnoDB Cluster는 여러 Replica 중 어느 노드를 새 Primary로 선택하는가?
- MySQL Router는 페일오버에서 어떤 역할을 하는가?

## PostgreSQL 페일오버

관련 개념: [[PostgreSQL 페일오버 설계 방식]]

- PostgreSQL은 자동 페일오버가 내장되어 있는가?
- WAL은 Primary와 Replica 사이에서 어떻게 전송되는가?
- Patroni는 어떤 역할을 하는가?
- 분산 코디네이터는 어떤 정보를 관리하는가?
- TTL 기반 리더 유지 방식은 어떻게 동작하는가?
- PgBouncer는 페일오버 맥락에서 어떤 역할을 하는가?
- 분산 코디네이터 자체가 SPOF가 되지 않게 하려면 어떻게 구성하는가?
- 동기 복제와 비동기 복제는 성능과 정합성 사이에서 어떤 트레이드오프를 가지는가?
- PgBouncer의 무중단 쿼리 버퍼링은 페일오버 중 어떤 역할을 하는가?
- Patroni 데몬과 WAL 전송 백그라운드 프로세스는 역할이 어떻게 다른가?
