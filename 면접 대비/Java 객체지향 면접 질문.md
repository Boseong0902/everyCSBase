# Java 객체지향 면접 질문

## 객체와 클래스

관련 개념: [[객체지향 프로그래밍의 개념 이해]], [[자바의 데이터 저장소 - 메서드 영역, 스택 영역, 힙 영역]]

- 객체란 무엇인가?
- 클래스란 무엇인가?
- 클래스와 객체의 차이는 무엇인가?
- 클래스는 인스턴스화하기 전까지 왜 힙 영역을 소모하지 않는가?
- 객체는 JVM 메모리 영역 중 어디에 저장되는가?
- 객체가 가비지 컬렉터에 의해 수집되는 기준은 무엇인가?

## 객체지향 4대 특성

관련 개념: [[객체지향 프로그래밍의 개념 이해]], [[OOP 4대 원칙]], [[interface]], [[정적 다형성과 런타임 다형성]]

- 추상화란 무엇인가?
- 캡슐화란 무엇인가?
- 상속은 어떤 장점과 위험을 가지는가?
- 다형성이란 무엇인가?
- 추상화와 다형성은 각각 제공자/호출자 관점에서 어떻게 다른가?
- 캡슐화는 단순히 필드를 private으로 만드는 것과 어떻게 다른가?
- 상속을 사용할 때 LSP를 함께 봐야 하는 이유는 무엇인가?
- 객체지향을 실세계에 빗대어 설명한다면 어떻게 설명할 수 있는가?
- 연관, 집약, 구성은 객체 간 관계에서 각각 어떻게 다른가?

## 정적 다형성과 런타임 다형성

관련 개념: [[정적 다형성과 런타임 다형성]], [[오버라이딩 vs 오버로딩]], [[interface]]

- 정적 다형성과 런타임 다형성의 차이는 무엇인가?
- 정적 디스패치와 동적 디스패치의 차이는 무엇인가?
- 오버로딩은 왜 런타임 다형성이 아닌가?
- 오버라이딩은 왜 런타임 다형성과 연결되는가?
- 인터페이스 타입으로 매개변수를 받으면 어떤 장점이 있는가?
- 가상 메서드란 무엇이며 어떤 메서드가 여기에 해당하지 않는가?
- 오버로딩 해석에서 컴파일러가 실제 객체가 아니라 참조 변수의 선언 타입을 기준으로 삼는 이유는 무엇인가?

## 오버라이딩과 오버로딩

관련 개념: [[오버라이딩 vs 오버로딩]], [[static, final 키워드]], [[LSP - 리스코프 치환]]

- 오버라이딩과 오버로딩의 주요 차이는 무엇인가?
- 오버라이딩과 오버로딩 각각의 규칙은 무엇인가?
- static 메서드를 오버라이드할 수 있는가?
- private 메서드를 오버라이드할 수 있는가?
- final 메서드를 오버라이드할 수 있는가?
- static 메서드를 오버로드할 수 있는가?
- 오버라이딩 메서드의 인수 목록을 변경해도 되는가?
- 공변 반환 타입 오버라이딩이란 무엇인가?
- 오버라이딩에서 throws 예외 범위는 왜 부모 메서드보다 커질 수 없는가?
- 바인딩이란 무엇인가?
- 정적 바인딩과 동적 바인딩의 차이는 무엇인가?
- 메서드 하이딩이란 무엇인가?
- 메서드 하이딩에서 호출될 메서드는 어떤 타입을 기준으로 결정되는가?
- 부모의 인스턴스 메서드를 자식이 static으로 바꾸면 왜 컴파일 에러가 나는가?
- 오버로딩에서는 왜 throws 선언에 제한이 없는가?
- 오버로딩된 메서드들은 왜 static 여부가 모두 같아야 하는가?
- 매개변수는 같고 반환 타입만 다른 메서드는 왜 오버로딩이 아닌가?
- 공변 반환 타입이 없다면 호출부에 어떤 불편이 생기는가?

## 상속과 컴포지션

관련 개념: [[상속보다 컴포지션]], [[OOP 4대 원칙]], [[인터페이스와 추상 클래스]]

- 컴포지션이란 무엇인가?
- 상속보다 컴포지션을 선호하라는 말은 무슨 뜻인가?
- 상속이 부모의 내부 구현에 의존하게 만든다는 말은 무슨 의미인가?
- 부모 구현이 바뀌면 자식이 깨질 수 있다는 문제는 어떤 상황에서 드러나는가?
- Java가 클래스 다중 상속을 지원하지 않는 것이 설계에 어떤 영향을 주는가?
- 인터페이스 구현이 상속보다 약한 결합을 만드는 이유는 무엇인가?
- 코드 재사용만을 목적으로 상속을 쓰면 어떤 문제가 생기는가?

## 인터페이스

관련 개념: [[interface]], [[인터페이스와 추상 클래스]], [[DIP - 의존 관계 역전]]

- 인터페이스란 무엇인가?
- 인터페이스는 추상화, 다형성, 느슨한 결합과 어떻게 연결되는가?
- 인터페이스를 사용하면 호출부가 왜 편해지는가?
- 구체 클래스에 직접 의존하면 어떤 문제가 생기는가?
- 인터페이스 타입으로 의존하면 결합도가 왜 낮아지는가?
- 클래스 내부에서 `new`로 의존 객체를 직접 만드는 것이 안티패턴인 이유는 무엇인가?

## 인터페이스와 추상 클래스

관련 개념: [[인터페이스와 추상 클래스]], [[interface]], [[static, final 키워드]]

- 인터페이스와 추상 클래스의 차이는 무엇인가?
- 인터페이스에는 왜 생성자가 없는가?
- 추상 클래스는 왜 상태를 가질 수 있는가?
- default 메서드는 왜 도입되었는가?
- 인터페이스의 static 메서드와 default 메서드의 차이는 무엇인가?
- 인터페이스 default 메서드를 남용하면 어떤 문제가 생기는가?
- abstract 메서드가 하나도 없는 추상 클래스를 만들 수 있는가?
- abstract이면서 동시에 final인 클래스를 만들 수 있는가?
- 인터페이스의 필드가 `public static final`로만 선언되는 이유는 무엇인가?
